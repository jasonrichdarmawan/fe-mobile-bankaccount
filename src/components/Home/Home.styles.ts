import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 10,
  },
  row: {
    flexDirection: 'row',
  },
  fontWeight: {
    fontWeight: 'bold',
  },
  wrapperPressable: {
    borderRadius: 8,
    margin: 10,
    padding: 20,
    flex: 1,
  },
});

export default styles;
